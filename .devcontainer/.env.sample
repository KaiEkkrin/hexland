# GPU Configuration for Hexland Dev Container
#
# This file configures which GPU (if any) the dev container should use.
# Copy this file to .env and uncomment ONE of the options below.
#
# NO GPU (default):
#   Don't create a .env file, or use:
#   COMPOSE_PROFILES=default
#
# NVIDIA GPU (WSL2 + Docker Desktop):
#   COMPOSE_PROFILES=nvidia
#
# AMD GPU (Native Linux with ROCm):
#   COMPOSE_PROFILES=amd

# Uncomment ONE of the following lines:
# COMPOSE_PROFILES=default
# COMPOSE_PROFILES=nvidia
# COMPOSE_PROFILES=amd
